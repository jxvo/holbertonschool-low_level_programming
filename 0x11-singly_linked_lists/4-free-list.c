#include <stdlib.h>
#include "lists.h"

/**
 * free_list - frees the allocated memory inside of a list_t list
 * @head: pointer to list struct of type list_t
 *
 * Return: nothing
 */
void free_list(list_t *head)
{
}
