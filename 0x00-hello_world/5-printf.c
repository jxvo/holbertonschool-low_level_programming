#include <stdio.h>

/**
 * main - prints a string
 * Description: prints a string with printf, followed by a new line
 * Return: returns zero
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
