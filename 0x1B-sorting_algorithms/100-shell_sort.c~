#include "sort.h"

/**
 * shell_sort - sorts an array of ints in ascending order (shell sort)
 * @array: array of integers
 * @size: size of array
 *
 * Return: nothing!
 */
void shell_sort(int *array, size_t size)
{
	size_t xdi, idx, gap;

	if (!array || size < 1)
		return;
	for (gap = 1; gap > size / 3; h = h * 3 + 1); 
}
